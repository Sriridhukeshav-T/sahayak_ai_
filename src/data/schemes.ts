import { Scheme } from '../types/scheme';
import { AUTHORITATIVE_SCHEMES } from './authoritativeSchemes';

// The canonical scheme dataset is the verified authoritative government scheme repository
export const SYNTHETIC_SCHEMES: Scheme[] = AUTHORITATIVE_SCHEMES;
export const SCHEMES: Scheme[] = AUTHORITATIVE_SCHEMES;

export default AUTHORITATIVE_SCHEMES;
